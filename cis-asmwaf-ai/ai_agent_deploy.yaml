apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-agent
  namespace: ai-workloads
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-agent
  template:
    metadata:
      labels:
        app: ai-agent
    spec:
      containers:
      - name: ai-agent
        image: quay.io/trinathsquay/ai-anomaly-agent:latest
        ports:
        - containerPort: 5000
        env:
        - name: KUBECONFIG # Environment variable name
          value: /etc/kubeconfig/config # Path inside the container
        volumeMounts:
        - name: kubeconfig-volume
          mountPath: /etc/kubeconfig
          readOnly: true # Important: Mount as read-only
      imagePullSecrets:
        - name: myquay
      volumes:
      - name: kubeconfig-volume
        secret:
          secretName: my-kubeconfig-secret # Name of your Secret

---

apiVersion: v1
kind: Service
metadata:
  name: ai-agent
  namespace: ai-workloads
spec:
  type: NodePort
  ports:
    - port: 514
      targetPort: 514
      protocol: UDP
      nodePort: 30514  # Manually defined NodePort
    - port: 5000
      targetPort: 5000
      protocol: TCP
      nodePort: 30500
  selector:
    app: ai-agent
